# Findings — AR 手势书写项目

## 技术调研结论

1. **MediaPipe Hands**: CPU 模式下单帧检测 10-25ms，21 点关键点输出稳定，适合实时应用。已知问题：低光照下抖动加剧，需 One-Euro Filter 平滑。

2. **手势识别方案**: 规则引擎（基于指尖距离/关节几何关系）优于分类器方案，原因：无需训练数据、可解释、可快速调参。风险：不同手型/距离的泛化有限。

3. **WebSocket vs WebRTC**: 本项目仅需传输关键点坐标（非视频流），WebSocket 足够且更简单。WebRTC 在此场景下属于过度设计。

4. **Canvas 渲染**: 分层 Canvas（历史层 + 活跃层 + 光标层）可有效控制重绘开销，避免全量重绘的性能陷阱。

5. **坐标对齐**: 摄像头 640×480 (4:3) 与浏览器窗口宽高比不一致，必须以视频渲染区域为基准做等比映射。

## 设计审查关键发现

- 后端与前端双源防抖导致状态分歧 → 手势判定统一收敛至前端
- Write/Switch 手势存在高误触风险 → 优先级链 + 死区时间 + Switch 仅食指独伸时评估
- 缺乏 Idle 态和性能预算 → 已补充
- 部署模型不明确 → 明确为纯本地部署

## 技术决策记录

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 手势判定位置 | 前端 | 单源真相，零网络延迟在判定回路中 |
| 平滑算法 | One-Euro Filter | 自适应截止频率，兼顾低延迟与平滑度 |
| 渲染方案 | 分层 Canvas 2D | 通用性好，分层策略控制重绘开销 |
| 部署模型 | 纯本地 | 隐私安全，简化安全约束 |
| WebSocket 协议 | ws://localhost | 本地环境下无需 wss:// |
